node_modules/
out/
dist/
.DS_Store
*.log
.env
.env.*
!.env.example

# Local secrets — never commit
settings.json
**/settings.json
mcp-auth.json
**/mcp-auth.json
*.pem
*.key
credentials*
secrets*

# Live-page scratch dumps (Steamworks/account HTML, cookies, etc.)
tmp-*
/tmp-*.mjs
/tmp-*.json

# Runtime browser profile — lives in %APPDATA%/sparo, not this repo
sessions.json
bookmarks.json
strategy-cache.json
/diag/
/traces/